import { Paper, styled } from '@mui/material';

export const MessageBgStyles = styled(Paper)({
    fontWeight: 'bold',
    padding: '10px 30px',
    borderRadius: '20px 20px 20px 0',
    width: 'fit-content',
    backgroundColor: '#306087',
});
