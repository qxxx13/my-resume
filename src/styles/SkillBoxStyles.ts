import { Box, styled } from '@mui/material';

export const SkillBoxStyled = styled(Box)({
    border: '2px solid blue',
    borderRadius: '10%',
    width: 'fit-content',
    padding: 5,
    marginRight: 10,
});
